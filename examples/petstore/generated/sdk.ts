// * auto-generated by @travjek/apig — do not edit manually
// * documentation - https://example.com



import type { PetList, Pet, CreatePetInput, Error, UpdatePetInput, OwnerList, Owner, CreateOwnerInput } from './zod';
import { ApigError } from './config';

/**
 * List all pets
 *
 * @param params
 */
export const listPets = async (params?: { limit?: number; status?: 'available' | 'pending' | 'sold' }): Promise<PetList> => {
  const r = await fetch(`/pets?${new URLSearchParams(params)}`);
  if (!r.ok) throw new ApigError(r.status, await r.json());
  return r.json() as Promise<PetList>;
};

/**
 * Create a pet
 *
 * @param body
 */
export const createPet = async (body: CreatePetInput): Promise<Pet> => {
  const r = await fetch(`/pets`, { method: 'POST', body: JSON.stringify(body), headers: { 'Content-Type': 'application/json' } });
  if (!r.ok) throw new ApigError(r.status, await r.json());
  return r.json() as Promise<Pet>;
};

export type GetPetErrors =
  | { status: 404; body: Error };

/**
 * Get a pet by ID
 *
 * @param id
 */
export const getPet = async (id: string): Promise<Pet> => {
  const r = await fetch(`/pets/${id}`);
  if (!r.ok) throw new ApigError(r.status, await r.json());
  return r.json() as Promise<Pet>;
};

/**
 * Update a pet
 *
 * @param id
 * @param body
 */
export const updatePet = async (id: string, body: UpdatePetInput): Promise<Pet> => {
  const r = await fetch(`/pets/${id}`, { method: 'PUT', body: JSON.stringify(body), headers: { 'Content-Type': 'application/json' } });
  if (!r.ok) throw new ApigError(r.status, await r.json());
  return r.json() as Promise<Pet>;
};

/**
 * Delete a pet
 *
 * @param id
 */
export const deletePet = async (id: string): Promise<void> => {
  const r = await fetch(`/pets/${id}`, { method: 'DELETE' });
  if (!r.ok) throw new ApigError(r.status, await r.json());
  return r.json() as Promise<void>;
};

/**
 * List all owners
 */
export const listOwners = async (): Promise<OwnerList> => {
  const r = await fetch(`/owners`);
  if (!r.ok) throw new ApigError(r.status, await r.json());
  return r.json() as Promise<OwnerList>;
};

/**
 * Create an owner
 *
 * @param body
 */
export const createOwner = async (body: CreateOwnerInput): Promise<Owner> => {
  const r = await fetch(`/owners`, { method: 'POST', body: JSON.stringify(body), headers: { 'Content-Type': 'application/json' } });
  if (!r.ok) throw new ApigError(r.status, await r.json());
  return r.json() as Promise<Owner>;
};

/**
 * Get an owner by ID
 *
 * @param id
 */
export const getOwner = async (id: string): Promise<Owner> => {
  const r = await fetch(`/owners/${id}`);
  if (!r.ok) throw new ApigError(r.status, await r.json());
  return r.json() as Promise<Owner>;
};
